version: "3.8"

services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://careline:careline_prod_2026@shared-postgres:5432/careline_prod
      JWT_SECRET: ${JWT_SECRET:-change-this-in-production}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://tbowo.top}
    ports:
      - "127.0.0.1:8002:8000"
    networks:
      - shared-net

networks:
  shared-net:
    external: true
